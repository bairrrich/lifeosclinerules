# 🔄 Active Context: Life OS

## Текущий фокус работы

**Сессия завершена успешно.** Все задачи Фазы 2 выполнены. Оптимизация производительности добавлена.

## Выполнено в этой сессии (2026-02-27)

### Фаза 2 — UI/UX улучшения
- ✅ **FAB кнопка** — плавающая кнопка "+" с выпадающим меню на 8 действий (еда, тренировка, финансы, вода, сон, настроение, книга, рецепт)
- ✅ **Streak на видном месте** — текущая серия отображается рядом с названием привычки
- ✅ **Копирование приёма пищи** — кнопка "Копировать" на детальной странице лога
- ✅ **Бюджеты по категориям** — BudgetManager с лимитами расходов, прогресс-бар, предупреждения

### Привычки
- ✅ **Негативные привычки** — тип habit_type ('positive' | 'negative')
- ✅ **Бейдж "Не делать"** — для негативных привычек
- ✅ **Текст "Нарушение"** — вместо "Пропуск" для негативных привычек

### Рецепты
- ✅ **Автоматический расчёт КБЖУ** — из ингредиентов с полями на 100г
- ✅ **Поддержка единиц** — г, кг, мл, л, ст.л., ч.л., стакан, шт
- ✅ **Кнопка "Рассчитать"** — в секции "Пищевая ценность"
- ✅ **Поля в одну строку** — Ккал, Белки, Жиры, Угл., Сахар

### Оптимизация производительности
- ✅ **@tanstack/react-virtual** — виртуализация списков
- ✅ **useCachedData hook** — кэширование запросов с TTL 5 минут
- ✅ **LazyLoad компонент** — отложенная загрузка при появлении в viewport
- ✅ **useIntersectionObserver hook** — определение видимости элемента

## Следующие шаги

### Приоритет — Синхронизация (Backend)
1. **API сервер** — Node.js/Express или Next.js API routes
2. **Authentication** — JWT или сессии
3. **Sync queue** — механизм очереди изменений
4. **Conflict resolution** — last-write-wins стратегия

### Приоритет — Дополнительные фичи
1. **Экспорт PDF отчётов** — для печати и архивирования
2. **Геймификация** — бейджи, достижения, мотивация
3. **Импорт из API** — Goodreads/Google Books по ISBN

## Активные решения и соображения

### Архитектура производительности
- **Виртуализация** — для списков > 100 элементов
- **Кэширование** — 5 минут TTL для частых запросов
- **Lazy loading** — для тяжёлых компонентов (графики, диаграммы)

### UI паттерны
- FAB для быстрых действий
- Tabs для переключения контекста
- Card секции для логической группировки
- Combobox для выпадающих списков с поиском
- Skeleton для состояний загрузки

## Важные паттерны и предпочтения

### Новые хуки и компоненты
```
src/hooks/use-cached-data.ts    → useCachedData, clearCache, invalidateCache
src/components/shared/virtual-list.tsx → VirtualList
src/components/shared/lazy-load.tsx    → LazyLoad, lazyLoad, useIntersectionObserver
src/components/shared/fab.tsx          → FAB
src/components/finance/budget-manager.tsx → BudgetManager
```

### Работа с данными
- Кэширование через useCachedData для частых запросов
- Инвалидация кэша при мутациях
- Виртуализация для больших списков

### Оптимизация рендеринга
- Lazy loading для графиков и диаграмм
- Skeleton вместо спиннеров
- Overscan 5 элементов для виртуализации